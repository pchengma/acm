#include<bits/stdc++.h>

using namespace std;

int main() {
    string s;
    
    // Iterate until the end:
    while(cin >> s);
    
    // Output size of the last word:
    cout << s.size();
    
    return 0;
}
